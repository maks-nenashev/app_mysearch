<%= form_for :thing, url: things_path, method: "thing" do %>

<br>
<br>
<div class="field is-link">
  <p class="control has-icons-left">
    <span class="select">
         
    <%#= select(:thing,:local_id, options_from_collection_for_select(@locals, "id","title", @thing.present? ? @thing.local_id : nil ), {}) %>
       <%= select(:thing,:local_id, Local.all.collect {|local| [local.title, local.id]}) %>  
    </span>
    <span class="icon is-small is-left">
      <i class="fas fa-globe"></i>
    </span>
  </p>
</div>

<div class="mb-3 row">
<div class="col-sm-2 col-form-label">  
    <%= label(:thing, :title) %>
  </div>

<div class="col-sm-10">         
    <%= text_field(:thing, :title, class: "input is-link") %>
  </div>
  </div>

<div class="mb-3 row">
 <div class="col-sm-2 col-form-label">
    <%= label(:thing, :text) %>
  </div>

<div class="col-sm-10">     
    <%= text_area(:thing, :text, class: "textarea is-focused") %>
  </div>
  </div>

<br>
<div class="col-sm-10">     
    <!%= file_field :thing, :avatar, class: "button is-warning"%>
  </div>
<br>
  
  <%= submit_tag 'Submit article', class: "button is-primary is-rounded"%>
  <%= link_to "Show all articles!",things_path, class:"button is-danger is-inverted" %> <!Prawelnaja ssylka>
  <%#= link_to "Show all articles!",things_path, class:"button big bi bi-pencil-square" %> <!Prawelnaja ssylka>

<%end%>

<br>
<br>
